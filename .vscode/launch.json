{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Manual Testing",
            "type": "PowerShell",
            "request": "launch",
            "script": "${workspaceFolder}/test/manual-test.ps1",
            "cwd": "${workspaceFolder}"
        }
    ]
}
